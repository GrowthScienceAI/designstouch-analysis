# Netlify redirects file
# https://docs.netlify.com/routing/redirects/

# Redirect default Netlify subdomain to primary domain (if you set up custom domain later)
# https://SITENAME.netlify.app/* https://yourdomain.com/:splat 301!

# SPA fallback - serve index.html for any unmatched routes
/*    /index.html   200
